<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ksmart.mapper.BoardMapper">
	<select id="getBoardList" resultType="kr.or.ksmart.domain.Board" parameterType="String">
		SELECT 
			board_no AS boardNo
			,board_title AS boardTitle
			,board_content AS boardContent
			,staff_code AS staffCode
			,board_wrt_date AS boardWrtDate
			,board_modi_date AS boardModiDate
		FROM
			board
		<if test="no neq null and no neq ''.toString() ">
		WHERE board_no = #{no}
		</if>
	</select>
</mapper>