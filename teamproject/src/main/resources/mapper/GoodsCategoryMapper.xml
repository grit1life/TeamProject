<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.or.ksmart.mapper.GoodsCategoryMapper">

	<resultMap type="Goods" id="goodsCategoryResultMap">
		<result property="goodsCategoryCode" column="goods_category_code"/>
		<result property="goodsCategoryName" column="goods_category_name"/>
	</resultMap>
	
	<!-- goods_category -->
	
	<insert id="insertGoodsCategory" parameterType="Goods">
	<selectKey order="BEFORE" keyProperty="goodsCategoryCode" resultType="String">
		SELECT
				CASE
					WHEN MAX(goods_category_code) LIKE 'cat_%'
					THEN
						CONCAT
							( 'cat_'
							, LPAD(MAX(CAST(SUBSTRING(goods_category_code, 5) AS DECIMAL))+1,'3','0') )
				ELSE
					ifnull(goods_category_code, 'cat_001')
				END AS goodsCategoryCode
			FROM
				goods_category;
	</selectKey>
		INSERT INTO goods_category
			(	goods_category_code
			, goods_category_name )
		VALUES
			(	#{goodsCategoryCode}
			, #{goodsCategoryName} );
	</insert>
	
	<select id="getGoodsCategoryList" resultMap="goodsCategoryResultMap">
		SELECT
				goods_category_code
			, goods_category_name
		FROM
				goods_category;
	</select>
	
	<update id="updateGoodsCategory" parameterType="Goods">
		UPDATE
				goods_category
			SET
				goods_category_name= #{goodsCategoryName}
			WHERE
				goods_category_code= #{goodsCategoryCode};
	</update>
	
	<delete id="deleteGoodsCategory" parameterType="String">
		DELETE FROM
			goods_category
		WHERE
			goods_category_code= #{goodsCategoryCode};
	</delete>
	
	<!-- goods_category END -->
	
</mapper>