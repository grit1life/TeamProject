<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="kr.or.ksmart.mapper.PeriodicInspectionMapper">
  
  	<!--정기 점검 현황 리스트  -->
	 <select id="getPeriodicInspectionList" parameterType="String" resultType="kr.or.ksmart.domain.PeriodicInspection">
		SELECT 
		 P.check_code AS checkCode
		,P.contract_code AS contractCode
		,P.branch_code AS branchCode
		,C.customer_id AS customerId
		,P.check_date AS checkDate
		FROM
		periodic_inspection AS P
		JOIN
		doc_contract AS C
		ON
		P.contract_code = C.contract_code;
		</select>
  </mapper>